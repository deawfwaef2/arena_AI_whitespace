# UI 重构 · 分阶段可玩存档

## 保存规则
- 根目录 `index.html` 始终是完整离线游戏，下载后双击打开，不需要服务器。
- 每次阶段修改后运行 `node scripts/checkpoint.mjs`：构建成功才原子替换根目录 HTML。
- Git 提交同时包含源代码和可玩的 HTML。不把密钥写入源码或构建。

## 阶段 1：恢复休息入口
- 修复 capital-ui.js 的确认休息处理器调用未导入的 beginRest，原先会提示 `beginRest is not defined` 而无法休息。
- 防止重复确认；只有成功进入休息才关闭确认窗口。
- 休息确认显示真实体力，不再一律显示 0。
- 补齐装饰相关按钮的动作分发白名单。
- 原有 22 项核心测试通过；下一阶段增加真实浏览器休息流程测试。

## 后续阶段
- 三套不同结构的 UI，带跨页面 / 财富阶段预览。
- 游戏画面内导航、弹窗与手机模拟视图。
- 回归检查：离线启动、休息、地图、投资、存档、布局边界。

## 阶段 2：三套布局与预览工作室
- 新增城市手账、夜行剧场、财富控制台三套实际游戏布局（分栏 / 底部行动台 / 侧栏），不是旧配色切换。
- 开始页可对比四个页面（街头、地图、休息、资产）、两种财富样例与桌面/手机尺寸。视觉样例明确标注，不修改真实游戏数据。
- 新增游戏内部统一导航与桌面手机模拟。地图、休息、身家、菜单、重选界面均在画面内。
- 统一弹窗容器、文字对比与面板过渡。富裕阶段新增私人事务面板、整体材质与字形变化。
- 开始按钮直接进入游戏，说明改为可选。
- 仍保留旧游戏逻辑、城市、音乐、资产和存档，未清空原有玩法。
- 下一步：浏览器回归，检查移动尺寸与完整休息流程。
