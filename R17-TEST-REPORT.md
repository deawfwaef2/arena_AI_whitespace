# R17 幽灵窗口修复 / 最终交付记录

日期：2026-09-25。游戏修复版本：`21bcdcd`（报告及测试脚本随后独立提交）。

## 下载 / 上传

- `broke-to-billionaire-crazygames.zip`：CrazyGames 上传包，入口 `index.html` 在 ZIP 根目录，不套多余文件夹。
- 离线玩：**解压整个 ZIP**，保留资源文件夹，然后双击 `index.html`，点击开始按钮。
- 包含 23 个文件：HTML、两个美术包、13 首 MP3、7 个音效包；约 30.90 MB（29.47 MiB）。
- 不需要 npm、服务器、CDN。CrazyGames SDK 只在平台环境启用；离线游戏与本地音乐不依赖它。
- 不清空原 `upshift-save-v3` 存档，不改经济数值和剧情选择。

## 复现与修复

1. **透明但挡住操作的窗口**：旧版 Chromium 实测 modal 为打开状态，卡片 opacity=0，入场动画 currentTime=0/startTime=null 持续数秒。新版本容器不再使用透明/位移入场和延迟退场；显示、关闭、inert、焦点、拖动清理由同一同步流程管理。剧情/按钮子元素动效保留。
2. **轻点标题窗口跳出屏幕**：旧版相对定位的居中 flex 弹窗被写入绝对坐标，小幅拖动重复叠加原居中位置。统一拖动实现切换到绝对定位后，以实际矩形校准，保持宽高，限制整个窗口在屏幕内。
3. **误关及残留状态**：背景遮罩不再点击即关；明确关闭按钮和普通窗口 Esc 保留；主线/解锁确认不能用背景或 Esc 跳过。关闭立即释放游戏 inert 并清理动画、旧坐标；blur/cancel/lost capture 结束拖动，resize 重置拖动布局。

## 实际验证结果

| 范围 | 结果 |
|---|---|
| `npm test` 规则测试 | 56 / 56，通过 |
| 第一阶段桌面 Chromium 1280×800 | 12 次菜单→子页→关闭 + 15 次快速关闭重开，通过；零页面脚本错误 |
| 第一阶段手机横屏模拟 844×390 | 同上完整压力流程通过，另含 CDP touch 真正触控事件 |
| **最终 ZIP** 解压、`file://`、阻断 HTTP/HTTPS | 完整 12 + 15 次压力流程通过；关闭后打工 0/24 → 3/24；本地 MP3 playing=true/error 为空；零页面脚本错误 |
| 最终版本手机横屏复查 | QUICK 模式 1 + 1 次流程，拖边、失焦、真正触控拖动、重开、resize、关闭后打工通过 |
| 最终版本 motion=false 复查 | QUICK 模式 1 + 1 次流程、拖动、重开及关闭后打工通过 |
| 主线对话 | Esc / 背景无法误关；选项确认正常关闭 |
| 拖动 | 2px 抖动不触发拖动；向左右/上/下边缘拖动仍在视口内；关闭重开归位 |
| ZIP 完整性 | CRC 通过；23 个包内文件逐字节等于工作区交付文件 |

- 截图已人工查看：桌面拖动、手机菜单/拖动/游戏；最终离线启动和游戏截图见 `qa/r17-evidence/`。
- 测试源码：`qa/r17-windows.mjs`；结果 JSON 与规则测试输出：`qa/r17-evidence/`。
- 浏览器使用软件 WebGL；为避免压力循环被 3D 渲染拖慢，测试脚本仅在循环期间节流 3D render，未修改 UI、事件或游戏逻辑，截图前恢复。该节流**不在交付 HTML 中**。
- 测试环境曾因同时保留两套 Chromium 安装而内存不足，引起截图超时及 WebGL 恢复重载；清理未使用浏览器后重新运行，以上最终结果来自成功完成的测试，不把失败尝试计为通过。
- 未声称覆盖实体 iPhone/Safari、所有随机后期事件、CrazyGames 后台审核或真实广告投放。本次是已复现窗口故障的修复及指定交互回归，不代表不存在其他未知 BUG。

## SHA-256

- `index.html`: `b3bd368e988cb2a57b178b2bef23990165248a66aaa3efc394939d5983f57d09`
- `broke-to-billionaire-crazygames.zip`: `03f5edfd144a8a8c638d67732d0f8a5c807af2be9f7e9fc88980248479c4ad19`
