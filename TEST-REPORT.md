# 实际检查记录

日期：2026-09-23。对象：Last $100: Swipe to Rich，3.1 财富与代价 / Windows 局域网版。

## 1. 构建

- `npm run build:production` 成功；图片、字体、音乐、运行脚本内置。
- 生产配置 `allowDeveloperMode=false`、平台 SDK 关闭，无运行时 CDN 依赖。
- Go 1.24.4 标准库编译 Windows x64 控制台 EXE；文件格式检查为 PE32+ x86-64。
- 同一 Go 源码另编译 Linux 服务器并实际运行，不是把 Linux 运行声称为 Windows 实机测试。

## 2. 核心规则：22 项通过

`npm test` / `qa/core.test.mjs`，结果见 `qa/core-results.txt`。

覆盖：初始现金 / 体力 / 五格健康 / 0% 风险；现金、资产、服装、商品与交割本金只计一次；当前财富退阶；全部门槛的上下边界；锁定休息账单、支付后当前身家下降而非峰值变化；无钱付账死亡且救助参数不能绕过；事件阻止提前付账；每次休息风险递增、自然上限 100%；体检刷新结果与幂等；健康耗尽死亡；三步合规、拒绝罚款；税款不双扣与拒税关系；安保策略 / 劫案 / 暗杀；奢侈消费不增加资产；奢侈点只结算一次、转世解锁；医疗扣款与风险降低；旧已付 / 未付休息存档迁移；街头事件确认后跨休息重载；96 项目与模型标识；五种财富起点的固定种子多阶段保存回归。

发现并修复了已确认街头事件被休息队列清理后导致存档失效的问题，以及罚款致死的现金快照顺序问题。

## 3. 浏览器操作与布局：4 组通过

Playwright Chromium，Linux 无头浏览器，软件 WebGL，移动视口与触屏模拟。结果见 `qa/browser-results.json`；这些不是实体手机测试。

实际覆盖视口：
- 390 × 844 手机竖屏；320 × 568 小屏。
- 844 × 390 横屏；1024 × 768 平板 / 小桌面；1440 × 900 桌面。
- 75% / 100% / 125% 界面比例。
- 贫穷、富裕、休息、体检选择、死亡结算状态。

实际操作包括：触屏滑杆选金额、文化说明展开 / 收起、断网后的下一站、跨转角、点击六城地图、选择东京、财富材质开关、奢侈消费确认、刷新后保留现金与开关、更多休息活动、休息镜头按钮、健康揭晓、刷新后继续已结算结果、死亡榜、购买 / 切换转世皮肤。

使用非安全 HTTP 主机名测试，验证局域网 HTTP 上 `crypto.randomUUID` 缺失时仍能生成下一站，而非只测试 localhost 安全上下文。正常游戏场景没有外部 HTTP 资源请求；测试过程未捕获页面 JavaScript 错误。

通过边界断言和截图检查，场景与操作面板不相交，现金文字不越出卡片，面板不出屏：
- 390px 手机普通 / 休息面板：x=12，宽366，高约388；场景在其上方另行占位。
- 320px 小屏面板：x=12，宽296，高约261；可滚动并保留前进 / 投资按钮。
- 1440px 桌面休息面板：x=20，宽354，位于左下；不再侵入右侧场景。

最初测试确实发现手机休息面板偏到屏幕外、电脑版休息面板宽度继承旧样式而压住场景。修正了旧样式优先级、translate、bottom 和现金长数字后，重新运行上述四组，全部通过。

## 4. 转角与场景专项：通过

`qa/world-check.mjs` 独立搭建相同 World 场景，连续检查两个转角。

- 采样证明人物先沿第一条街移动，再沿垂直方向移动，不只是镜头变化。
- 每个转角摄像机方向增加 90 度。
- 期间相邻街区数量保持不变。
- 转角后人物回到与新街道一致的位置；项目同步朝向新街道。
- 招牌的世界朝向与摄像机一致，避免旋转父级后文字变成侧面。
- 无页面异常。结果见 `qa/world-results.json`。

软件渲染首次着色器预热会很慢，专项先预热再采样；没有把测试帧率当作手机性能指标。

## 5. 启动器 / 成品检查：通过

`launcher/main_test.go` 检查私有、回环、链路本地来源允许，公开地址拒绝；`qa/final-check.mjs` 检查：
- GET 返回与最终 HTML SHA-256 完全一致的内容，支持 gzip。
- HEAD 正常，只有响应头。
- 任意源码路径、不存在文件和穿越路径为 404，POST 为 405。
- 最终成品开始界面：进入转世事务所、买皮肤、换肤、返回开始、进入游戏、实际投资和自动保存。

## 未声称覆盖

- 没有在实体 Windows 上运行 EXE；没有实际触发 Windows 防火墙 / UAC / SmartScreen。可选防火墙助手仅做源码核查，不冒充原生执行测试。
- 没有实体 iPhone、Android、Safari、家庭路由器或 VPN / AP 隔离环境实测。
- 没有长时间挂机、所有随机排列穷举、真实平台 SDK 验分或手机音频听感测试。
- 已验证浏览器操作和数值回归，不承诺所有设备满帧、所有未知旧存档或恶意改档都可恢复。

ZIP 中保留测试源码与最终文本结果；截图是工作检查材料，不把整个测试环境、浏览器或依赖打包给玩家。
