# Last $100: Swipe to Rich
## Windows 电脑开服，手机同 Wi-Fi 游玩

1. 完整解压压缩包。
2. 双击 **Start-Last100.exe**，不需要安装 Python / Node / Go。
3. 电脑会打开游戏，程序窗口会显示手机地址，例如 `http://192.168.1.25:8765/`。
4. 手机连接同一个 Wi-Fi，输入**你电脑实际显示的地址**。不要输入电脑专用的 `127.0.0.1`。
5. 保持电脑开机、程序窗口开启；关闭窗口即停止服务。

**如果连接不上**：检查访客 Wi-Fi 隔离及 Windows 防火墙。首次权限提示只允许「专用网络」。需要时可运行可选的 `Allow-Private-Network.cmd`，它会征求同意，仅放行此程序、专用网络、同一子网。不要关闭防火墙或设置路由器端口转发。

**保存**：手机和电脑分别保存，不自动同步。更换 IP、端口或浏览器也会改变存档空间；可通过游戏菜单导出 / 导入迁移。

**单机**：电脑也可直接打开 `index.html`，但推荐通过启动器进入以获得稳定的网页地址。

**声音**：首次点击游戏后开始播放；主菜单可以静音和调音量。

**界面**：主菜单可调整面板 / 文字、关闭财富配色贴图。小屏面板可以上下滑动；投资滑杆保持现有操作。

完整玩法、素材授权、编译方法见 `README.md`；实际检查范围见 `TEST-REPORT.md`。Windows EXE 未签名、未在实体 Windows 上执行；手机检查为 Chromium 触屏与视口模拟，不冒充真机实测。
