# 模型交接 / 必须先读

## 用户的硬性约束（每次编程前重读本文件）
- 根目录 `index.html` 必须始终是完整、自包含、下载后双击即可开始玩的离线游戏，不依赖 npm、服务器或外部 CDN。
- 先打包可玩 HTML，再继续下一阶段。每个小阶段运行 `npm run checkpoint`，构建成功才原子替换根 HTML；提交源码时必须同时提交 HTML。
- 经常分阶段保存并推送 GitHub，不能最后才打包/推送。推送失败须如实说明，本地仍必须留可玩版本。
- 不覆盖/清空原有用户存档；保留原玩法，增量完善。货币内部单位是美分，界面美元。
- 不把任何密钥写进仓库、HTML、日志、交接或下载包；本次对话中暴露的访问令牌应由用户撤销更换。

## 设计要求（优先于旧版三布局方案）
- **所有游戏 UI 必须在游戏画面内**，不做画面外分栏、管理控制台或大块仪表盘。
- **贫穷阶段极简**：现金/阶级提示、机制解锁进度条、当前投资决策。辅助功能用很小的画内菜单；不要初始铺满地图、资产、排名等选项。
- 财富增长后逐步解锁界面，清晰预告下一机制门槛，带入场动画和明确解锁提醒；不要闪现闪退。
- 城市全景在主游戏内按资产阶段显示，不得仅藏进世界地图；地图是旅行用途。
- 手机优先保护场景可见面积，投资卡紧凑、触控目标可点、弹窗可滚动，文本对比固定，不能随环境变暗而看不清。
- 路边要有来自不同地域/阶级/派系的行人：头顶财富、阶级、阵营；思想气泡根据玩家身家及关系变化。现实地域不决定道德或性格。
- 项目负责人有对玩家的评价和对白，项目内容及外观体现城市差异。
- 人才市场招募随从；随从有实际机制、维护费、可解雇。点击场景中的随从才打开其管理页，不能增加常驻随从管理栏。
- 操作要有即时按压/数值/结果反馈，转场有动效；尊重减少动态效果设置。
- 根据用户下一轮反馈迭代，不声称未实现的内容已完成。

## 工作流程
1. 读 HANDOFF.md、PROGRESS.md、git status。
2. 修改一个可独立交付阶段。
3. `npm run checkpoint`（生产版禁用调试），根目录 HTML 是交付物。
4. 检查相关逻辑和浏览器离线/手机布局。测试结果只写真实已执行的。
5. 更新 PROGRESS.md，commit 并 push origin 当前分支。随后才进入下一阶段。

## 当前接手记录（2026-09-23）
- 从 c39c951 接手，旧版根 HTML 已存在约 32 MB。
- 用户拒绝旧「城市手账 / 夜行剧场 / 财富控制台」的分栏布局；本轮转为单一沉浸街景。
- 参考 ZIP 已从用户提供的 Drive 下载，需检查其中 UI 结构，不执行不明脚本。
- 旧游戏已有 six-city 内容、财富阶段、维护账单和解锁机制，应复用而非另造简化演示游戏。

## 最新可玩版本：5.0 沉浸街头（2026-09-23）
- 根 HTML 已多次打包并推送，GitHub Pages 与本机 HTML 的 SHA-256 已核对一致。仍用原 upshift-save-v3，本轮未改存储键。
- 实际主界面只保留 street 布局，旧 journal/theater/console 设置在 applyLayout 中迁移。不要恢复用户拒绝的分栏布局。
- UI：`src/redesign.js/.css`，上下文人物/市场/随从：`src/street-ui.js`；纯规则：`src/street-core.js`；3D 角色和锚点：`src/world.js`。
- 人才最多 3 人。guide 前进耗能 4；merchant 普通街头倍率 +0.10；analyst 普通项目概率 +3（双重尽调前应用）；producer 普通倍率 +0.15；steward 基础维护 −15%；diplomat 打招呼关系 +4；medic 健康风险 −3%。不改当前已生成项目条款。
- 招募资格按当前总身家；工资每次休息计入 maintenance，其中 companions 为明细，不要再次加到总账单。休息锁账后不能招募/解雇。
- 拍卖 AUCTION_LOTS 的 price/upkeep 是美分，**不同于普通资产 price 的美元参考数值**。显示不能再乘 100；奖励字段是 points。旧 auction 对象已兼容迁移成 id。
- 思想气泡每 9 秒固定选择一次，不能每帧根据当前可见标签改选，否则会出现布局振荡。人物标签 hover 后位置固定，保证点击。
- 构建：`npm ci && npm run checkpoint`。测试：`npm test`；浏览器：先 `npm start`，再 `node qa/street-browser.mjs`，需 Playwright Chromium 和系统依赖。
- 最后验证：43 项逻辑测试、9 组 Chromium 浏览器回归全部通过；file:// 拦截全部 HTTP(S) 请求后依然可开局投资，网络请求为 0，页面错误为 0。
- 截图及完整报告见 `STREET-REPORT.md` 和 `qa/street-final-*.png`。

## 下一轮优先关注
- 让用户评价实际手机的场景可见面积、标签密度、投资卡位置，不擅自加回整排常驻选项。
- 当前同时显示的行人标签为手机 1–2 个、桌面 2–4 个，场景实际人物更多；这是有意防遮挡，不是缺失数据。
- 区域差异复用原 6 城/72 项本地内容，本轮增强卡片强调色、项目经理和当地人才。进一步独立城市建筑造型/更多事件属于后续美术与内容迭代。
- 当前自动验证仅 Chromium，未声称经过真机 iOS/Safari、Android 低端机、全数后期随机事件长时压力测试。真机反馈后继续调整。
- 单文件约 32 MiB，内置音频/字体/图片，无外部请求；初次加载成本高于拆分在线版，不要为了减文件体积破坏离线要求。

## 新反馈优先级 / 6.0 大字与戏剧化反馈（本轮必须遵守）
- 用户明确否定 5.0 的微小 UI：主信息、人物与店主气泡、按钮都必须显著放大，不靠极小字节省空间。
- 地图/旅行等已解锁入口放左上，保持财富解锁，不能开局全给。沿画面边缘布置，不回到画面外分栏。
- 体力要明显条形；机制进度要变大，放画面底部/左下，明确下一项解锁是什么。
- 人才市场是沿途随机地点，不是常驻按钮。离开地点不能继续招募；已签约者随行且仍需点人物管理。
- 地区投资卡采用不同的图案/背景图片，不只是换颜色；文字对比优先，图片全部嵌入离线 HTML。
- 财富达标：有预热、解锁动效、明确停留与确认。财富回落：阶级装饰/临时权限回收必须播放退场动效；已购买永久道具不要凭 UI 回收误删存档。
- 新增随地域和财富阶级变化的当地事件，选择有明确实际后果，结算一次且存档后不能刷新重抽。
- 休息账单改固定阶级费用：阶级门槛 30%（生存阶级门槛 0，采用 $8 基础生活费）；另收固定休息服务费（门槛 3%，生存 $2）。不再按当前总资产比例征税或收管理费。随从/保镖/收藏等明示额外维护继续保留。
- 金钱旁展示下次扣费及门槛；“扣费后保级需现金”要考虑非现金资产，不把身家误称现金。
- 开始画面主要按钮居中；弹窗有入场和退场过渡；点击有按压、波纹/音效反馈，尊重减少动态设置。
- 转过街角时镜头短暂环绕并缓动回到稳定构图，避免一直朝同一侧或突然旋转。
